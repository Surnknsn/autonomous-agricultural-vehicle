# AMR Description

This package was converted from the SolidWorks URDF export in
`/home/iai/Downloads/assem_urdf.zip`.

The current `base_link` frame is still the CAD export origin. Before using this
for navigation, verify the frame orientation and sensor offsets in RViz against
the real mower.
